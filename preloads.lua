-- If savestate have to be load before a tas, you configure it here
-- Requirements : table (key : tas name [a folder name in the tas folder], value : name of the savestate [have to be present in your savestate folder])
return {}